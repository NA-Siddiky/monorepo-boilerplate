{
  "name": "@root/core",
  "private": true,
  "scripts": {
    "android": "cd apps/android/unzip_blog_app && yarn start",
    "web": "cd apps/web/unzip_blog && yarn dev",
    "build": "yarn lerna run build",
    "clean": "yarn lerna clean"
  },
  "workspaces": {
    "packages": [
      "apps/**",
      "packages/**"
    ],
    "nohoist": [
      "**/*"
    ]
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}